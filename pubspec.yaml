name: untitled1
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.4.1 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cached_network_image: ^3.4.1
  supabase_flutter: ^2.5.3

  geolocator: ^12.0.0
  permission_handler: ^11.3.1

  cupertino_icons: ^1.0.8
  flutter_native_splash: ^2.4.0
  device_preview: ^1.2.0
  image_picker: ^1.1.2
  esewa_flutter: ^3.0.1
  flutter_inappwebview: ^6.1.5
  crypto: ^3.0.6
  flutter_dotenv: ^5.1.0 # ADDED THIS

  flutter_map: ^6.1.0
  latlong2: ^0.9.1
  
  # Firebase dependencies for push notifications
  firebase_core: ^3.1.1
  firebase_messaging: ^15.0.3
  flutter_local_notifications: ^17.1.2
  
  provider: ^6.1.2
  intl: ^0.20.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter_native_splash:
  color: '#FFFFFF'
  image: assets/DOOKO.png
  android_12:
    image: assets/DOOKO.png
    ios: true

flutter:
  uses-material-design: true
  assets:
    - assets/
